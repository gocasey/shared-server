{
  "name": "shared-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "test": "nyc --check-coverage --lines 75 --functions 75 --branches 75 --reporter=html --reporter=text mocha --recursive"
  },
  "dependencies": {
    "express": "^4.16.3",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "mocha": "^5.0.4",
    "nyc": "^11.6.0",
    "proxyquire": "^2.0.1",
    "sinon": "^4.4.6",
    "supertest": "^3.0.0"
  }
}
