{
  "name": "shared-server",
  "main": "src/app.js",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint src test",
    "test": "mocha --recursive",
    "coverage": "nyc --all --check-coverage --lines 75 --functions 75 --branches 75 --reporter=html --reporter=text npm test",
    "start": "node ./src/app.js",
    "debug": "NODE_ENV=development node --inspect-brk=34939 src/server.js"
  },
  "dependencies": {
    "config": "^1.30.0",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "request": "^2.85.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^4.19.0",
    "eslint-config-google": "^0.9.1",
    "mocha": "^5.0.5",
    "nyc": "^11.6.0",
    "sinon": "^4.5.0",
    "sinon-chai": "^3.0.0"
  }
}
